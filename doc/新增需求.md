新增角色管理，新增管理员角色/普通用户角色/未来可能拓展超极管理员角色，管理员可以查看AdminView.vue,管理员登录时展示，主要用于查看所有用户的上传html，审核，修改子域名等操作。
1.AdminVue采用侧边路由的方式，侧边导航+右侧主体
2.AdminVue主要有：
    用户管理，当前仅实现查看所有用户的昵称邮箱表格
    HTML管理，
        搜索：昵称/邮箱/子域名/公开私密/审核状态。
        查看所有用户上传的html表格。
        操作：查看html，审核未审核/通过/拒绝(审核字段是不是不应该用bool)，删除，修改子域名
子组件形式放到web\src\components\Admin\中

新增过滤条件，vue过滤style1
新增过滤函数体


//想要添加一些边界防止一些恶意使用操作？
1，设置用户上传的html大小不能超过1MB
2，设置用户上传存储时不能超过100kb
3，默认用户上传html数量未删除的不能超过20个，累计上传的不能超过100个(包括删除了的)
当然特殊情况可以联系管理员拓展上限，但是默认不允许

⏱️ 2. 上传频率限流（防批量刷接口、CC 攻击）
单用户1 分钟内最多上传 3 次
单用户1 小时内最多上传 10 次
同一内容（哈希值相同）5 分钟内禁止重复上传（防冗余刷量）
单 IP 每日总上传请求 **≤500 次 **（防 IP 恶意攻击）


安全隔离
HTML 净化库，


【任务类型】功能开发 / 后端
【核心目标 / 问题】为HTML上传功能添加4项默认边界限制：HTML内容大小≤1MB、存储数据≤100KB、未删除上传数≤20个、累计上传数≤100个，未来管理员可手动拓展上限
【明确前提 & 假设】基于现有Go+Gin+Gorm项目架构；仅修改HTML上传/存储核心逻辑；不改动用户认证、数据库基础配置；管理员拓展上限实现方式需用户确认
【只允许查看】service/api/html_record.go、service/service/html_record.go、service/dao/html_record.go、service/service/html_record_data.go、service/model/html_record.go
【不要做】不要全项目扫描 / 目录遍历；不要修改与核心目标无关的任何代码；不要自动执行全量构建 / 全量测试 / 依赖安装；不要循环迭代修复；不要新增未在核心目标中提及的功能；不要修改前端代码；不要改动软删除、JWT认证逻辑
【已知信息】1.后端使用Go+Gin+Gorm框架 2.上传接口：/api/html/upload 3.HTML记录存html_record表 4.支持软删除 5.数据存储存html_record_data表
【验收标准】1.HTML内容超1MB上传接口返回错误 2.存储数据超100KB上传同步接口返回错误 3.未删除上传≥20个禁止上传 4.累计上传≥100个禁止上传 5.限制为默认值，不影响管理员拓展 6.仅修改核心文件，无无关代码改动
【输出要求】先给核心结论 / 方案概述，再列具体修改点；代码改动遵循最小化原则，仅修改核心目标相关内容；所有代码修改必须标注对应文件路径；不输出与核心目标无关的冗余解释


