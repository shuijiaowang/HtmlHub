新增过滤条件，vue过滤style1
新增过滤函数体


新增功能：
帮我添加一些简单的接口限流，如暴力登录，IP限制，不需要redis，简单缓存就行，我也没有那么多用户，也没啥很值钱的数据？

【任务类型】代码重构
【核心目标 / 问题】将 html_record.go 中 injectSyncScript 函数内的长脚本字符串拆分到 service/script 目录独立文件，添加简单说明，支持最小化防止遮挡
【明确前提 & 假设】仅拆分脚本字符串，不修改业务逻辑；保持原有功能完全不变；文件路径统一规范
【只允许查看】service/api/html_record.go，service/script
【不要做】不要全项目扫描 / 目录遍历；不要修改与核心目标无关的任何代码；不要自动执行全量构建 / 全量测试 / 依赖安装；不要循环迭代修复；不要新增未在核心目标中提及的功能；不要修改除 injectSyncScript 外的其他函数
【已知信息】injectSyncScript 包含两段长脚本：同步助手面板、访问校验页面；需拆分到外部文件并嵌入
【验收标准】1. 成功创建 script 目录及对应 .js 文件；2. 原代码功能完全一致，无报错；3. 脚本可最小化，有简单注释说明；4. 仅修改 injectSyncScript 相关代码
【输出要求】先给核心结论 / 方案概述，再列具体修改点；代码改动遵循最小化原则，仅修改核心目标相关内容；所有代码修改必须标注对应文件路径；不输出与核心目标无关的冗余解释





