

个人HTML记录里，新增删除按钮。点击可删除记录，并删除其他用户的存储记录，伪删除。


修改表结构，新增一个公开私密，创建时默认为私密审核状态，私密状态时需要从主页点击跳转，请求携带token？，解析为上传者才返回html？同理审核状态也是，仅自己可看，外人无法查看。
但这样的话，用户想要打开自己的私密网页的话，都需要从主页跳转，这并不好？
我能想到的方法就是，用户第一次需要从主页跳转，并把token存储local中。如何是私密的网页的话，用户ABC请求时，先发送一个检查脚本，查找本地token携带二次请求，A解析成功后才返回html，后续就不需要检查了，除非token过期，再跳转到主页？







