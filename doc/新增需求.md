新增过滤条件，vue过滤style1
新增过滤函数体


新增功能：
1.可修改简介，
2.可更新html。更新html需要用户重新上传htnl其余值不变，但会把审核状态改为未审核。
3.对用户上传的html进行简单筛查，必须是完整的html，不要随便一句话都能上传。


【任务类型】功能开发 / 前端 / 后端
【核心目标 / 问题】实现 HTML 记录简介修改、HTML 内容更新（重置审核状态为未审核）、新增完整 HTML 格式校验功能防止上传非html内容
【明确前提 & 假设】基于现有 Go+Gin 后端、Vue3+ElementPlus 前端项目；仅修改 HTML 记录相关逻辑；更新 HTML 时仅替换 HTML 内容，其余字段不变，审核状态改为 pending；HTML 必须包含完整<html><head><body>标签才算合法
【只允许查看】service/api/html_record.go、service/service/html_record.go、service/model/html_record.go、web/src/views/ManageView.vue、web/src/api/html.js
【不要做】不要全项目扫描 / 目录遍历；不要修改与核心目标无关的任何代码；不要自动执行全量构建 / 全量测试 / 依赖安装；不要循环迭代修复；不要新增未提及的功能；不要修改用户、定时任务、数据库基础配置代码
【已知信息】项目为 HTML 上传管理系统；HtmlRecord 模型包含 description、approvalStatus、htmlContent 字段；审核状态：pending = 未审核、approved = 通过、rejected = 拒绝；现有上传接口仅做非空校验
【验收标准】1. 前端可编辑并保存 HTML 简介；2. 更新 HTML 接口仅修改 htmlContent，审核状态自动改为 pending；3. 后端校验 HTML 必须为完整结构，否则返回错误；4. 校验失败前端展示明确提示；5. 原有功能正常运行无影响
【输出要求】先给核心结论 / 方案概述，再列具体修改点；代码改动遵循最小化原则；所有代码修改标注对应文件路径；不输出与核心目标无关的冗余解释





