


【任务类型】后端修改
【核心目标/问题】创建表结构
【明确前提&假设】列清前置判断，不确定项直接提问
【只允许查看】service\model\project.go
【不要做】不要全项目扫描；不要改无关代码；不要跑全量构建，不要自动执行全量测试；不要循环修复；不要测试我会手动测试的。
【已知信息】报错/现象/复现步骤/已有能力（最多5行，极简表述）
【验收标准】可量化、可验证的成功条件（拒绝“能用就行”）
【输出要求】先给核心结论，再给修改点；代码量改动尽量小，不要过度设计。


【任务类型】仅前端优化
【核心目标/问题】
管理页`点击打开`是存放这链接吗，为什么右键可以复制链接，我需要的是必须点击之后才会把token复制上传，防止小白把token一起分享了
【允许查看】前端web
【不要做】不要全项目扫描；不要跑全量构建，不要自动执行全量测试；不要循环修复。
【输出要求】代码量改动适度，不要过度设计。


【任务类型】前端、功能开发、修 Bug
【核心目标 / 问题】在上传 HTML 页与「管理-更新 HTML」弹窗中，缓解手机浏览器/QQ 内大段粘贴卡顿，粘贴后光标与视口回到编辑区顶部，并增加「复制」「粘贴」「清空」按钮且行为一致。
【明确前提 & 假设】不改动后端接口与请求字段；沿用现有 Vue3 + Vite 前端栈；剪贴板 API 在 QQ WebView/部分移动浏览器可能需权限或不可用，失败时仅提示、不静默失败（具体文案需用户确认）；卡顿根因未用性能工具在仓库内验证，优化手段以可落地的前端改动为准（需用户确认是否接受「仅程序化粘贴路径优化」或「系统粘贴同样要优化」）。
【只允许查看】e:/code/HtmlHub/web/src/views/UploadView.vue、e:/code/HtmlHub/web/src/views/ManageView.vue、e:/code/HtmlHub/web/package.json
【不要做】不要全项目扫描 / 目录遍历；不要修改与核心目标无关的任何代码；不要自动执行全量构建 / 全量测试 / 依赖安装；不要循环迭代修复；不要新增未在核心目标中提及的功能；不要更换为富文本编辑器或重构整页布局；不要引入未说明的大型依赖。
【已知信息】UploadView 用原生 textarea 绑定 uploadForm.htmlContent。ManageView「更新 HTML」用 el-input textarea 绑定 htmlDraft。用户反馈手机端与 QQ 内大段粘贴卡顿。粘贴后光标在底部、上传按钮在顶部需长距离滚动。需在上述两处增加复制/粘贴/清空。
【验收标准】上传页 HTML 文本区与 ManageView「更新 HTML」弹窗均出现「复制」「粘贴」「清空」；清空后对应 v-model 为空且可正常提交/保存。点击「粘贴」在成功时写入剪贴板文本，失败时可见错误提示。大段内容写入后插入点位于文本起始位置，且编辑区域可见（视口无需再滚到页面最底找光标）。在「需用户确认」的机型与 QQ 版本上，大段粘贴后 3 秒内可滚动页面并再次操作上传/按钮（或与改前对比主线程阻塞明显改善）。
【输出要求】核心结论：仅在 UploadView、ManageView 做 UI 与粘贴后选区/滚动及可选的赋值路径优化，避免波及其他页面。具体修改点：UploadView.vue 为 textarea 增加工具行（粘贴/清空）、paste 后 setSelectionRange(0,0) 与容器 scrollIntoView/scrollTop；ManageView.vue 在更新 HTML 的 el-input 旁或插槽内加同等按钮与相同逻辑（Element Plus 需取内部 textarea ref）；卡顿侧优先减少大文本触发的多余响应式/同步重排（实现细节在文件内最小化）；所有改动标注文件路径；不写与上述无关的说明。

