首页优化，拆分路由行。
上传html页：提示，前缀，上传html文件/html文本，给用户使用， 按钮 `上传并写入数据库` 仅保留`上传`即可，文件名也不需要手动输入。
上传html记录页面：基本保持不变，添加删除按钮，点击可以删除，对应的用户存储记录页会被删掉（伪删除）。
新增一个主页，纯静态，讲解一下`使用教程`,`如何问AI:请用html+css+js+local存储生成一个完整的html页面，需求是：...`,`联系方式：QQ群..微信..`
新增一个页面，用于展示一些别人都上传了什么：暂时不开放，保留页面。
