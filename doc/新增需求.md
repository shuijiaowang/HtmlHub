新增角色管理，新增管理员角色/普通用户角色/未来可能拓展超极管理员角色，管理员可以查看AdminView.vue,管理员登录时展示，主要用于查看所有用户的上传html，审核，修改子域名等操作。
1.AdminVue采用侧边路由的方式，侧边导航+右侧主体
2.AdminVue主要有：
    用户管理，当前仅实现查看所有用户的昵称邮箱表格
    HTML管理，
        搜索：昵称/邮箱/子域名/公开私密/审核状态。
        查看所有用户上传的html表格。
        操作：查看html，审核未审核/通过/拒绝(审核字段是不是不应该用bool)，删除，修改子域名
子组件形式放到web\src\components\Admin\中

新增过滤条件，vue过滤style1
新增过滤函数体


【任务类型】功能开发,前端,后端
【核心目标 / 问题】新增角色管理（管理员/普通用户/预留超级管理员），仅管理员可访问AdminView.vue；实现侧边路由布局的Admin页面，完成用户管理表格、HTML管理（多条件搜索+查看/审核/删除/改子域名）功能，子组件存放至指定目录
【明确前提 & 假设】
1. 角色分为管理员、普通用户，预留超级管理员拓展位，修改jwt逻辑；
2. AdminView采用侧边导航+右侧主体布局；
3. HTML审核字段考虑不使用bool改用状态`未审核/通过/拒绝`；
4. 子组件必须存放于web/src/components/Admin/；
5. 基于现有Go+Vue3项目开发，不破坏原有功能
【只允许查看】service，web
【不要做】不要全项目扫描/目录遍历；不要修改与核心目标无关的任何代码；不要自动执行全量构建/全量测试/依赖安装；不要循环迭代修复；不要新增未在核心目标中提及的功能；不要修改原有用户注册登录核心逻辑
【已知信息】1. 项目技术栈：Go后端+Vue3+Element Plus；2. 现有用户模型无角色字段；3. 现有HTML模型isApproved为bool类型需修改；4. 需新增管理员权限校验中间件/逻辑；5. 前端路由需新增管理员页面权限控制
【验收标准】
1. 用户表新增角色字段，成功区分管理员/普通用户；
2. 仅管理员登录后可访问AdminView.vue，普通用户无权限；
3. AdminView实现侧边导航+右侧主体的布局样式；
4. 用户管理模块简单展示所有用户的昵称、邮箱，表格分页形式；
5. HTML管理支持按昵称/邮箱/子域名/公开私密/审核状态搜索；
6. HTML管理实现查看、审核、删除、修改子域名操作
7. 所有管理员相关子组件均存放于web/src/components/Admin/目录；
8. 权限校验生效，接口与页面均做管理员权限控制
【输出要求】先给核心结论/方案概述，再列具体修改点；代码改动遵循最小化原则，仅修改核心目标相关内容；所有代码修改必须标注对应文件路径；不输出与核心目标无关的冗余解释





